import type { Styles } from 'types';

const styles: Styles = {
  root: {
    paddingTop: 40,
    textAlign: 'center'
  },
  form: {
    paddingRight: '0 !important',
    paddingLeft: '0 !important',
    display: 'flex',
    marginBottom: 20,
    gap: 10
  }
};

export default styles;
