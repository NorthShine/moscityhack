import type { Styles } from 'types';

const styles: Styles = {
  root: {
    paddingTop: 70,
    textAlign: 'center',
    maxWidth: 400
  },
  title: {
    marginBottom: 20
  },
  input: {
    marginBottom: 20
  }
};

export default styles;
