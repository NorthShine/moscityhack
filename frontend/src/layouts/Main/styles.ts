import type { Styles } from 'types';

const styles: Styles = {
  root: {
    height: '100vh'
  },
  container: {
    padding: 20,
    backgroundColor: '#9fa7d6',
    borderRadius: '10px'
  }
};

export default styles;
